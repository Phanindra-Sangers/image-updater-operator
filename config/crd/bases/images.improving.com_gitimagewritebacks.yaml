---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: gitimagewritebacks.images.improving.com
spec:
  group: images.improving.com
  names:
    kind: GitImageWriteback
    listKind: GitImageWritebackList
    plural: gitimagewritebacks
    singular: gitimagewriteback
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.url
      name: URL
      type: string
    - jsonPath: .spec.branch
      name: Branch
      type: string
    - jsonPath: .status.lastCommitSHA
      name: LastCommit
      type: string
    - jsonPath: .status.lastRunTime
      name: LastRun
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GitImageWriteback is the Schema for the gitimagewritebacks API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of GitImageWriteback
            properties:
              auth:
                description: |-
                  auth configures Git credentials. May be omitted for public repositories
                  over HTTPS.
                properties:
                  httpsSecretRef:
                    description: |-
                      httpsSecretRef references a Secret with keys "username" and "password"
                      (a personal access token works as the password). For GitHub fine-grained
                      or classic tokens, set username to your user or token name.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                  sshSecretRef:
                    description: |-
                      sshSecretRef references a Secret with key "identity" (PEM private key),
                      optional "known_hosts", and optional "password" (key passphrase). Use this
                      with an SSH clone URL such as git@github.com:org/repo.git.
                    properties:
                      name:
                        minLength: 1
                        type: string
                    required:
                    - name
                    type: object
                type: object
              branch:
                default: main
                description: branch is the branch to read and write. Defaults to "main".
                type: string
              commit:
                description: commit configures the commit produced when image values
                  change.
                properties:
                  authorEmail:
                    default: image-updater@improving.com
                    description: authorEmail is the Git author/committer email.
                    type: string
                  authorName:
                    default: image-updater-operator
                    description: authorName is the Git author/committer name.
                    type: string
                  messageTemplate:
                    default: |-
                      chore(images): automated image update

                      {{updates}}
                    description: |-
                      messageTemplate is the commit message. The token {{updates}} is replaced
                      with a newline-separated list of "file: policy -> value" entries.
                    type: string
                  push:
                    default: true
                    description: |-
                      push, when true (default), pushes the commit to the remote branch. When
                      false the commit is made locally only (useful for dry runs in tests).
                    type: boolean
                type: object
              interval:
                default: 5m
                description: interval is how often the repository is checked. Defaults
                  to 5m.
                type: string
              path:
                default: .
                description: |-
                  path is the directory within the repository to scan for marker comments.
                  Defaults to "." (the whole repo).
                type: string
              policies:
                description: |-
                  policies optionally restricts which ImagePolicy names this writeback will
                  apply. When empty, any policy named by a marker is honored.
                items:
                  type: string
                type: array
              suspend:
                description: suspend pauses the writeback when true.
                type: boolean
              url:
                description: |-
                  url is the Git remote, HTTPS (https://host/org/repo.git) or SSH
                  (git@host:org/repo.git). The auth method must match the URL scheme.
                minLength: 1
                type: string
            required:
            - url
            type: object
          status:
            description: status defines the observed state of GitImageWriteback
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the GitImageWriteback resource.
                  Condition type "Ready" is True after a successful run.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastCommitSHA:
                description: lastCommitSHA is the SHA of the last commit this controller
                  pushed.
                type: string
              lastRunTime:
                description: lastRunTime is when the repository was last checked.
                format: date-time
                type: string
              observedGeneration:
                description: observedGeneration is the .metadata.generation last reconciled.
                format: int64
                type: integer
              updatedImages:
                description: updatedImages lists the marker updates applied at the
                  last successful run.
                items:
                  type: string
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
